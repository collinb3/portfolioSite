# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored (see .gitignore's `*.local` rule) — never commit real secrets.

# Neon Postgres connection string (from the Neon dashboard).
POSTGRES_CONNECTION_STRING=

# Shared password for the /booking/admin owner dashboard.
ADMIN_PASSWORD=
